enum ProductMatch {
    MATCHED = 'MATCHED',
    NOT_MATCHED = 'NOT_MATCHED',
    IN_PROGRESS = 'IN_PROGRESS',
}

enum AdItemMatchType {
    PRODUCT = 'product',
    PRODUCT_GROUP = 'product-group',
    BRAND = 'brand',
}

export { ProductMatch, AdItemMatchType };
